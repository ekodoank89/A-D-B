package io.github.libxposed.api

/**
 * STUB compile-only.
 * Implementasi asli disediakan framework LSPosed saat runtime
 * (di-inject ke classloader), sehingga stub ini hanya dipakai saat kompilasi
 * dan otomatis tertimpa (shadowed) oleh versi asli di runtime.
 */
interface XposedInterface

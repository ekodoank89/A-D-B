package de.robv.android.xposed

/**
 * STUB compile-only — signature WAJIB identik dengan framework asli.
 * @JvmStatic agar compile ke invokestatic (framework asli = static method).
 */
object XposedBridge {
    @JvmStatic
    fun log(text: String) {
        // no-op (stub)
    }
}
